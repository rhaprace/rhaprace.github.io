export const PARTICLE_CONFIG = {
  COUNT: 200,
  SPREAD: 10,
  SPEED: 0.1,
  BASE_SIZE: 100,
  MOVE_ON_HOVER: true,
  ALPHA_PARTICLES: false,
  DISABLE_ROTATION: false,
} as const;

export const PARTICLE_COLORS = {
  DARK: ["#ffffff", "#e0e0e0"] as string[],
  LIGHT: ["#000000", "#333333"] as string[],
};

